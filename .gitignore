# API Key 等敏感配置，绝不提交
config.json
.env
# PM2 配置里也写了 API Key，同样排除
ecosystem.config.js

# 账号数据（含密码哈希）
users.json

# token 签名密钥（首次启动自动生成，泄露可伪造登录态）
.session_secret

# 系统文件
.DS_Store
node_modules/
server.log
